en:
  activerecord:
    models:
      integration/purchases/configuration:
        one: Finished bidding configurations

    attributes:
      integration/purchases/configuration:
        headers_soap_action: SOAPAction
        password: Password
        user: User
        wsdl: WSDL
        operation: SOAP method name
        response_path: Response path
        month: Month

