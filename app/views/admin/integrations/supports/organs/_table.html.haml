.table-responsive
  %table.table.table-hover.table-sm.table-sorted{summary: t('.summary')}

    %thead

      %tr

        - heads = [:codigo_entidade, :descricao_entidade, :codigo_orgao, :sigla, :descricao_orgao, :data_inicio, :data_termino, :orgao_sfp]

        - heads.each_with_index do |head, index|
          - sort_column_name = sort_columns[index]

          %th{ class: "#{sortered_table_active_class(sort_column_name)}" }
            - if sort_column_name
              = sortered_table_header(sort_column_name, t(".heads.#{head}"))
            - else
              = t(".heads.#{head}")

    %tbody
      %tbody
        = render partial: 'organ', collection: organs

  .text-center
    = paginate organs
