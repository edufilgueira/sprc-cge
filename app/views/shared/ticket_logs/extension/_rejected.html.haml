%h4.h6.mb-0
  = t('.title', user: responsible_or_user(ticket_log).as_author)

%small
  %em
    = l(ticket_log.created_at, format: :shorter)

%p
  = raw(ticket_log.description)

%hr{'aria-hidden': 'true'}
