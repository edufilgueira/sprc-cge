- content_for :breadcrumbs, render_breadcrumbs(breadcrumbs)
- content_for :title, t(".title")

- stats_url = transparency_expenses_budget_balances_path(show_stats: true)
- filter_path = 'expenses/budget_balances'

= render 'shared/transparency/index', transparency_id: :expenses_budget_balances, stats_type: :month_range, stats_url: stats_url, base_locale: t('.'), filter_path: filter_path
