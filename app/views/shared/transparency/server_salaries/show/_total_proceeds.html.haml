%table.table.table-hover.table-striped.table-sm.my-0{summary: t('.summary')}

  %tbody

    %tr.font-weight-bold
      %td
        = t('shared.transparency.server_salaries.show.total_proceeds.total')
      %td.w-25
        = number_to_currency(server_salary.income_final)

    %tr
      %td
        = t('shared.transparency.server_salaries.show.total_proceeds.income_dailies')
      %td
        = number_to_currency(server_salary.income_dailies)
