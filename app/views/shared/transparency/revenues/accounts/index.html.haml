- content_for :breadcrumbs, render_breadcrumbs(breadcrumbs)
- content_for :title, t(".title")

- stats_url = transparency_revenues_accounts_path(show_stats: true)

= render 'shared/transparency/index', transparency_id: :revenues_accounts, stats_type: :month_range, stats_url: stats_url, base_locale: t('.')
