- content_for :stylesheet, stylesheet_link_tag(stylesheet)
- content_for :breadcrumbs, render_breadcrumbs(breadcrumbs)
- content_for :title, t(".title")

- stats_url = transparency_revenues_registered_revenues_path(show_stats: true)
- filter_path = 'revenues/registered_revenues'

= render 'shared/transparency/index', transparency_id: :revenues_registered_revenues, stats_type: :year, stats_url: stats_url, base_locale: t('.'), filter_path: filter_path
