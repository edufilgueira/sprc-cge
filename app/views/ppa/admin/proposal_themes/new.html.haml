- content_for :title, t('.title')
- content_for :breadcrumbs, render_breadcrumbs(breadcrumbs)

.content-detail

  .card.card-custom

    .card-header

      %h2.card-title
        = t(".title")

    .card-block

      = render 'form', ppa_proposal_theme: ppa_proposal_theme, url: ppa_admin_proposal_themes_path(ppa_proposal_theme)
